const config = {
  screens: {
    Home: {
      path: 'home',
    },
    Cart: {
      path: 'cart',
    },
  },
};

const linking = {
  prefixes: ['vetta://demo'],
  config,
};

export default linking;
